export default function capitalize(word) {
  return word[0].toUpperCase() + word.substr(1).toLowerCase();
}

// Capitalize function created and exported by export default to ./greet.js
// this function was imported just by the ./greet.js, and is being used by ./index.js through ./greet.js
